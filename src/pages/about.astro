---
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="About VantaJax"
  description="Learn what VantaJax is, why it exists, and how it supports Black-owned businesses in Jacksonville."
>
  <div class="bg-white">
    <!-- HERO (luxury) -->
    <section class="relative overflow-hidden px-4 pt-14 pb-10">
      <!-- background layer -->
      <div class="pointer-events-none absolute inset-0">
        <!-- parallax glows -->
        <div
          class="vj-glow vj-glow-a absolute -top-28 left-1/2 h-72 w-[46rem] -translate-x-1/2 rounded-full bg-purple-600/12 blur-3xl"
          aria-hidden="true"
        ></div>
        <div
          class="vj-glow vj-glow-b absolute top-12 left-1/4 h-56 w-96 -translate-x-1/2 rounded-full bg-fuchsia-500/10 blur-3xl"
          aria-hidden="true"
        ></div>

        <!-- soft paper gradient -->
        <div class="absolute inset-0 bg-linear-to-b from-slate-50 via-white to-white"></div>

        <!-- subtle vignette -->
        <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,rgba(88,28,135,0.06),transparent_55%)]"></div>
      </div>

      <div class="relative mx-auto max-w-3xl">
        <div
          class="vj-panel vj-panel-hero rounded-[28px] border border-slate-200/70 bg-white/70 p-7 shadow-[0_12px_30px_-18px_rgba(2,6,23,0.35)] backdrop-blur"
          data-reveal
          data-delay="0"
        >
          <div class="flex flex-wrap items-center gap-3">
            <span class="inline-flex items-center rounded-full border border-purple-200/80 bg-purple-50/70 px-3 py-1 text-[11px] font-semibold uppercase tracking-[0.22em] text-purple-700">
              About VantaJax
            </span>
            <span class="text-xs text-slate-500">
              Built for Jacksonville • Local-first • Trust-driven
            </span>
          </div>

          <h1 class="mt-4 text-3xl sm:text-4xl font-semibold tracking-tight text-slate-900">
            Visibility is infrastructure.
          </h1>

          <p class="mt-3 text-sm sm:text-base text-slate-600 leading-relaxed">
            VantaJax is a local-first radar for Black-owned businesses, services, and professionals
            across Jacksonville, FL — built to make support easier, more intentional, and more real.
          </p>

          <!-- Proof strip -->
          <div class="mt-6 grid grid-cols-1 sm:grid-cols-3 gap-3">
            <div class="vj-pill rounded-2xl border border-slate-200/70 bg-white/60 p-4">
              <p class="text-xs font-semibold uppercase tracking-[0.22em] text-slate-500">Trust</p>
              <p class="mt-1 text-sm text-slate-700">Verification-first approach</p>
            </div>
            <div class="vj-pill rounded-2xl border border-slate-200/70 bg-white/60 p-4">
              <p class="text-xs font-semibold uppercase tracking-[0.22em] text-slate-500">Access</p>
              <p class="mt-1 text-sm text-slate-700">Free to discover & share</p>
            </div>
            <div class="vj-pill rounded-2xl border border-slate-200/70 bg-white/60 p-4">
              <p class="text-xs font-semibold uppercase tracking-[0.22em] text-slate-500">Local</p>
              <p class="mt-1 text-sm text-slate-700">Built for Jax, expanding intentionally</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CONTENT -->
    <section class="px-4 pb-14">
      <div class="mx-auto max-w-3xl space-y-8">

        <!-- THE PROBLEM -->
        <div class="vj-panel" data-reveal data-delay="70">
          <header class="flex items-start gap-3">
            <span class="vj-icon">
              <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="8"></circle>
                <circle cx="12" cy="12" r="3"></circle>
                <path d="M12 2v2M12 20v2M2 12h2M20 12h2"></path>
              </svg>
            </span>
            <div>
              <h2 class="vj-h2">The problem</h2>
              <div class="vj-rule"></div>
            </div>
          </header>

          <div class="mt-5 space-y-4 text-sm sm:text-base text-slate-700 leading-relaxed">
            <p>
              Supporting Black-owned businesses shouldn’t require insider knowledge, social media algorithms,
              or word-of-mouth luck. Yet too often, discovery is fragmented, outdated, or buried beneath platforms
              that prioritize trends, ads, and pay-to-play visibility.
            </p>
            <p>
              People want to support. Businesses want to be found. The gap between those two is what VantaJax exists to close.
            </p>
          </div>
        </div>

        <!-- MISSION -->
        <div class="vj-panel vj-panel-soft" data-reveal data-delay="140">
          <header class="flex items-start gap-3">
            <span class="vj-icon">
              <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M12 21s7-4.35 7-10A7 7 0 1 0 5 11c0 5.65 7 10 7 10z"></path>
                <path d="M12 11l3-3"></path>
              </svg>
            </span>
            <div>
              <h2 class="vj-h2">Our mission</h2>
              <div class="vj-rule"></div>
            </div>
          </header>

          <p class="mt-5 text-sm sm:text-base text-slate-700 leading-relaxed">
            Our mission is to make Black-owned businesses discoverable, trusted, and supported —
            starting locally, and expanding intentionally.
          </p>
        </div>

        <!-- PRINCIPLES (SMOKED GLASS) -->
        <div class="vj-panel vj-panel-signature" data-reveal data-delay="210">
          <header class="flex items-start gap-3">
            <span class="vj-icon vj-icon-signature">
              <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M12 2l7 4v6c0 5-3 9-7 10-4-1-7-5-7-10V6l7-4z"></path>
              </svg>
            </span>
            <div>
              <h2 class="vj-h2 vj-h2-signature">What guides this platform</h2>
              <div class="vj-rule vj-rule-signature"></div>
            </div>
          </header>

          <ul class="mt-6 grid gap-4 sm:grid-cols-2">
            {[
              {
                title: "Trust over traffic",
                body:
                  "We prioritize accuracy, verification, and community confidence — not virality, ads, or engagement games.",
              },
              {
                title: "Community over extraction",
                body:
                  "No paid placement. No influencer rankings. No selling access to attention. This exists to reduce friction, not profit from it.",
              },
              {
                title: "Access is non-negotiable",
                body:
                  "Discovery should be free and open. Visibility shouldn’t depend on who can pay.",
              },
              {
                title: "Built to uplift, open to all",
                body:
                  "Anyone can use the platform — but its purpose is clear: uplift and support Black-owned businesses and professionals.",
              },
            ].map((v) => (
              <li class="vj-card vj-card-signature group">
                <p class="font-semibold text-white/95">{v.title}</p>
                <p class="mt-2 text-sm text-white/70 leading-relaxed">{v.body}</p>
                <div class="mt-4 h-px w-10 bg-purple-400/30 transition-all duration-200 group-hover:w-16 group-hover:bg-purple-300/60"></div>
              </li>
            ))}
          </ul>
        </div>

        <!-- HOW IT WORKS -->
        <div class="vj-panel vj-panel-soft" data-reveal data-delay="280">
          <header class="flex items-start gap-3">
            <span class="vj-icon">
              <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M12 2l10 6-10 6L2 8l10-6z"></path>
                <path d="M2 16l10 6 10-6"></path>
                <path d="M2 12l10 6 10-6"></path>
              </svg>
            </span>
            <div>
              <h2 class="vj-h2">How VantaJax works</h2>
              <div class="vj-rule"></div>
            </div>
          </header>

          <ol class="mt-6 space-y-5">
            <li class="flex gap-4">
              <span class="vj-step">1</span>
              <div class="pt-1">
                <p class="font-semibold text-slate-900">Discover</p>
                <p class="mt-1 text-sm sm:text-base text-slate-600 leading-relaxed">
                  Browse businesses by location, category, and verification status.
                </p>
              </div>
            </li>

            <li class="flex gap-4">
              <span class="vj-step">2</span>
              <div class="pt-1">
                <p class="font-semibold text-slate-900">Contribute</p>
                <p class="mt-1 text-sm sm:text-base text-slate-600 leading-relaxed">
                  Suggest businesses that should be on the radar. Submissions are reviewed to protect quality and trust.
                </p>
              </div>
            </li>

            <li class="flex gap-4">
              <span class="vj-step">3</span>
              <div class="pt-1">
                <p class="font-semibold text-slate-900">Build confidence</p>
                <p class="mt-1 text-sm sm:text-base text-slate-600 leading-relaxed">
                  Over time, verification helps create a directory people can rely on — not just scroll through.
                </p>
              </div>
            </li>
          </ol>
        </div>

        <!-- VISION -->
        <div class="vj-panel" data-reveal data-delay="350">
          <header class="flex items-start gap-3">
            <span class="vj-icon">
              <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M9 18l-6 3V6l6-3 6 3 6-3v15l-6 3-6-3z"></path>
                <path d="M9 3v15"></path>
                <path d="M15 6v15"></path>
              </svg>
            </span>
            <div>
              <h2 class="vj-h2">Where this is going</h2>
              <div class="vj-rule"></div>
            </div>
          </header>

          <div class="mt-5 space-y-4 text-sm sm:text-base text-slate-700 leading-relaxed">
            <p>
              VantaJax starts as a directory, but the long-term vision is bigger: a mapping-based experience that helps people
              discover Black-owned businesses in real time, in the places they already are.
            </p>
            <p>
              Over time, that foundation can grow into an ecosystem — connecting communities, events, partnerships, and shared visibility
              without losing the trust it was built on.
            </p>
          </div>
        </div>

        <!-- CTA -->
        <div class="vj-cta" data-reveal data-delay="420">
          <div class="text-center">
            <p class="text-base sm:text-lg font-semibold text-slate-900">
              Trust is built together.
            </p>
            <p class="mt-3 text-sm sm:text-base text-slate-700 leading-relaxed">
              If you know a business that should be here, suggest it. If this platform feels useful, share it with someone who would care.
            </p>
            <p class="mt-4 font-medium text-slate-900">
              Visibility grows when people pass it forward.
            </p>

            <div class="mt-6 flex flex-col sm:flex-row gap-3 justify-center">
              <a href="/suggest" class="vj-btn vj-btn-primary">Suggest a business</a>
              <a href="/browse" class="vj-btn vj-btn-secondary">Browse the directory</a>
            </div>
          </div>
        </div>

      </div>
    </section>
  </div>

  <!-- Scroll reveal + luxury parallax -->
  <script is:inline>
    (() => {
      const reduceMotion =
        window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;

      // Scroll reveal
      const els = Array.from(document.querySelectorAll("[data-reveal]"));
      if (els.length) {
        if (reduceMotion) {
          els.forEach((el) => el.classList.add("vj-reveal-in"));
        } else {
          els.forEach((el) => {
            el.classList.add("vj-reveal");
            const d = el.getAttribute("data-delay");
            if (d) el.style.transitionDelay = `${d}ms`;
          });

          const io = new IntersectionObserver(
            (entries, observer) => {
              entries.forEach((entry) => {
                if (!entry.isIntersecting) return;
                entry.target.classList.add("vj-reveal-in");
                observer.unobserve(entry.target);
              });
            },
            { threshold: 0.12, rootMargin: "0px 0px -10% 0px" }
          );

          els.forEach((el) => io.observe(el));
        }
      }

      // Luxury parallax glows (subtle)
      if (reduceMotion) return;

      const glowA = document.querySelector(".vj-glow-a");
      const glowB = document.querySelector(".vj-glow-b");
      if (!glowA && !glowB) return;

      let raf = 0;
      const onScroll = () => {
        if (raf) return;
        raf = requestAnimationFrame(() => {
          raf = 0;
          const y = window.scrollY || 0;

          if (glowA) glowA.style.transform = `translate3d(-50%, ${Math.min(18, y * 0.02)}px, 0)`;
          if (glowB) glowB.style.transform = `translate3d(-50%, ${Math.min(14, y * 0.015)}px, 0)`;
        });
      };

      window.addEventListener("scroll", onScroll, { passive: true });
      onScroll();
    })();
  </script>

  <style>
    /* Scroll reveal */
    [data-reveal].vj-reveal {
      opacity: 0;
      transform: translate3d(0, 10px, 0);
      filter: blur(6px);
      transition:
        opacity 700ms ease,
        transform 700ms ease,
        filter 700ms ease;
      will-change: opacity, transform, filter;
    }
    [data-reveal].vj-reveal.vj-reveal-in,
    [data-reveal].vj-reveal-in {
      opacity: 1;
      transform: translate3d(0, 0, 0);
      filter: blur(0);
    }

    /* Base luxury system */
    .vj-panel {
      border-radius: 28px;
      border: 1px solid rgba(226, 232, 240, 0.75);
      background: linear-gradient(180deg, rgba(255,255,255,0.92), rgba(255,255,255,0.86));
      padding: 28px;
      box-shadow: 0 12px 30px -22px rgba(2, 6, 23, 0.45);
      transition: transform 200ms ease, box-shadow 200ms ease;
    }
    .vj-panel:hover {
      transform: translateY(-2px);
      box-shadow: 0 16px 38px -24px rgba(2, 6, 23, 0.55);
    }
    .vj-panel-soft {
      background: linear-gradient(180deg, rgba(248,250,252,0.95), rgba(255,255,255,0.85));
    }

    .vj-icon {
      margin-top: 2px;
      display: inline-flex;
      height: 36px;
      width: 36px;
      align-items: center;
      justify-content: center;
      border-radius: 18px;
      background: rgba(147, 51, 234, 0.10);
      color: rgba(126, 34, 206, 0.95);
      box-shadow: inset 0 0 0 1px rgba(147, 51, 234, 0.12);
    }

    .vj-h2 {
      font-size: 0.875rem;
      font-weight: 600;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: rgba(148, 163, 184, 1);
    }
    .vj-rule {
      margin-top: 8px;
      height: 1px;
      width: 56px;
      background: rgba(147, 51, 234, 0.55);
    }

    .vj-card {
      border-radius: 20px;
      border: 1px solid rgba(226, 232, 240, 0.75);
      background: rgba(255,255,255,0.9);
      padding: 20px;
      box-shadow: 0 10px 24px -20px rgba(2, 6, 23, 0.45);
      transition: transform 200ms ease, box-shadow 200ms ease;
    }
    .vj-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 14px 30px -22px rgba(2, 6, 23, 0.55);
    }

    .vj-step {
      display: inline-flex;
      height: 36px;
      width: 36px;
      align-items: center;
      justify-content: center;
      border-radius: 18px;
      background: rgba(255,255,255,0.92);
      border: 1px solid rgba(226, 232, 240, 0.85);
      color: rgba(126, 34, 206, 0.95);
      font-weight: 700;
      box-shadow: 0 10px 24px -22px rgba(2, 6, 23, 0.45);
    }

    .vj-cta {
      border-radius: 28px;
      border: 1px solid rgba(233, 213, 255, 0.9);
      background: linear-gradient(180deg, rgba(250,245,255,0.95), rgba(255,255,255,0.9));
      padding: 28px;
      box-shadow: 0 14px 34px -24px rgba(2, 6, 23, 0.5);
    }

    .vj-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 18px;
      padding: 12px 20px;
      font-size: 0.875rem;
      font-weight: 700;
      text-decoration: none;
      transition: transform 180ms ease, box-shadow 180ms ease, background-color 180ms ease;
      outline: none;
    }
    .vj-btn:focus {
      box-shadow: 0 0 0 3px rgba(147, 51, 234, 0.25);
    }
    .vj-btn-primary {
      background: rgba(147, 51, 234, 1);
      color: white;
      box-shadow: 0 14px 30px -20px rgba(147, 51, 234, 0.55);
    }
    .vj-btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 18px 34px -22px rgba(147, 51, 234, 0.62);
      background: rgba(126, 34, 206, 1);
    }
    .vj-btn-secondary {
      background: rgba(255,255,255,0.92);
      color: rgba(15, 23, 42, 1);
      border: 1px solid rgba(203, 213, 225, 0.9);
      box-shadow: 0 14px 30px -24px rgba(2, 6, 23, 0.45);
    }
    .vj-btn-secondary:hover {
      transform: translateY(-1px);
      box-shadow: 0 18px 34px -26px rgba(2, 6, 23, 0.55);
      background: rgba(248,250,252,1);
    }

    /* ===== Signature “smoked glass” band (replaces black glass) ===== */
    .vj-panel-signature {
      position: relative;
      border: 1px solid rgba(148, 163, 184, 0.22);
      background:
        radial-gradient(900px 500px at 10% -10%, rgba(147, 51, 234, 0.18), transparent 55%),
        radial-gradient(700px 400px at 110% 30%, rgba(217, 70, 239, 0.10), transparent 55%),
        linear-gradient(180deg, rgba(15, 23, 42, 0.72), rgba(15, 23, 42, 0.62));
      box-shadow:
        0 16px 42px -30px rgba(2, 6, 23, 0.65),
        inset 0 1px 0 rgba(255, 255, 255, 0.05);
      overflow: hidden;
    }
    .vj-panel-signature::before {
      content: "";
      position: absolute;
      inset: 0;
      background:
        linear-gradient(135deg, rgba(255,255,255,0.06), transparent 40%),
        radial-gradient(700px 260px at 50% 0%, rgba(255,255,255,0.05), transparent 60%);
      pointer-events: none;
    }
    .vj-panel-signature::after {
      content: "";
      position: absolute;
      inset: 0;
      background-image: radial-gradient(rgba(255,255,255,0.05) 1px, transparent 1px);
      background-size: 22px 22px;
      opacity: 0.04;
      pointer-events: none;
    }

    .vj-icon-signature {
      background: rgba(147, 51, 234, 0.14);
      color: rgba(226, 232, 240, 0.9);
      box-shadow: inset 0 0 0 1px rgba(216, 180, 254, 0.14);
    }

    .vj-h2-signature {
      color: rgba(226, 232, 240, 0.7);
    }
    .vj-rule-signature {
      background: rgba(216, 180, 254, 0.45);
    }
    .vj-muted-signature {
      color: rgba(226, 232, 240, 0.6);
    }

    .vj-card-signature {
      border: 1px solid rgba(148, 163, 184, 0.22);
      background: rgba(30, 41, 59, 0.55);
      box-shadow:
        0 14px 36px -28px rgba(2, 6, 23, 0.7),
        inset 0 1px 0 rgba(255,255,255,0.04);
      backdrop-filter: blur(10px);
    }
    .vj-card-signature:hover {
      transform: translateY(-1px);
      box-shadow:
        0 18px 44px -32px rgba(2, 6, 23, 0.8),
        inset 0 0 0 1px rgba(216, 180, 254, 0.10);
    }

    /* Nicer text rendering */
    .vj-panel, .vj-card, .vj-cta {
      -webkit-font-smoothing: antialiased;
      text-rendering: optimizeLegibility;
    }
  </style>
</Layout>
