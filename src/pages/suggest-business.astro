---
import Layout from "../layouts/Layout.astro";
import SuggestBusinessForm from "../components/SuggestBusinessForm";
---

<Layout
  title="Suggest a Black-owned business • VantaJax"
  description="Submit a Black-owned business in Jacksonville, FL to help grow the VantaJax directory."
>
  <section class="flex h-full items-center justify-center">
    <div
      class="w-full max-w-3xl rounded-3xl border border-slate-200 bg-white px-6 py-8 sm:px-8 sm:py-10 shadow-sm"
    >
      <div class="mb-5 sm:mb-6 space-y-2">
        <p class="text-[11px] text-slate-500">
          <a
            href="/"
            class="text-purple-700 hover:text-purple-900 underline underline-offset-2"
          >
            ← Back to home
          </a>
        </p>
        <h1 class="text-2xl sm:text-3xl font-semibold tracking-tight">
          Suggest a Black-owned business
        </h1>
        <p class="mt-1 max-w-xl text-sm text-slate-700">
          Know a spot that should be on the radar? Submit the details below.
          We’ll review and add verified businesses to the directory.
        </p>
      </div>

      <!-- Quick actions -->
      <div class="mb-7">
        <div
          class="mx-auto max-w-md rounded-2xl border border-slate-200 bg-slate-50 px-4 py-4 sm:px-5 sm:py-5"
        >
          <p class="text-[11px] font-semibold text-slate-500 mb-3 uppercase tracking-[0.18em]">
            Quick actions
          </p>

          <div class="flex justify-center">
            <a
              href="/businesses"
              class="inline-flex items-center justify-center rounded-full bg-purple-600 px-6 py-2.5 text-xs sm:text-sm font-semibold text-white shadow-sm hover:bg-purple-700 transition"
            >
              Browse businesses
            </a>
          </div>
        </div>
      </div>

      <section>
        <SuggestBusinessForm client:load />
      </section>
    </div>
  </section>
</Layout>
